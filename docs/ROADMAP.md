# План реализации оставшихся калькуляторов (52 из 69)

Готово 17 (паровая группа). Осталось 52. Фундамент (IAPWS-IF97, единицы, трубы, движок,
гидравлика, модель клапанов) уже построен и переиспользуется — поэтому остальное пойдёт быстрее.

## Что нужно для каждого калькулятора
1. **Формула** — `CALC['<код>']` в `calc-formulas.js` (для большинства — простая алгебра).
2. **Страница** — свёрстанный `calc-N.pug` с разметкой `data-*`. ← основной объём работ
   (52 новые страницы; нет в проекте). Рекомендуется **генерировать из шаблона**, т.к. входы/выходы/единицы
   известны из `formulas/tlv_app_reverse/calculators.json`.
3. **Ссылка** на странице-меню `calc-main.pug`.
4. **Калибровка** по toolbox.tlv.com (для большинства не требуется — формулы полные).

## Общие «строительные блоки» (сделать один раз, до фаз)
- **Свойства воды** ρ(T), μ(T) — для гидравлики воды (плотность можно брать из IF97 область 1; вязкость жидкой воды — отдельная корреляция, в IF97-модуле она сейчас не годится при высокой плотности).
- **Свойства воздуха/газа** — идеальный газ ρ=p·M/(R·T), вязкость воздуха.
- **Психрометрия влажного воздуха** — для 14500, 14600.
- **Генератор pug-страниц** из шаблона + автодобавление в меню.
- Доп. категории единиц: площадь, энергия, коэф. теплоотдачи/теплопроводности (для конверторов).

---

## Фаза 1 — «почти бесплатно» (16 шт., минимум физики)
- **151xx Конверторы единиц (13)** — ✅ ГОТОВО. Страницы `conv-*.html` (температура, разность темп.,
  давление, длина, площадь, объём, скорость, масса, вязкость, энергия, теплоотдача, теплопроводность,
  энтальпия). Движок: форма `data-convert="<кат>"`, поле `data-conv-in`, вывод в `[data-conv-results]`
  через `Units.list()`. Стиль `.conv-row` в `_calc-page.scss`. Ссылки в меню calc-main.
- **11350 / 11360 Stall point (2)** — ✅ ГОТОВО. Страница `calc-18.html` (общая для жидкости/газа,
  формула одна). Сверено с TLV точно: StallPoint=(Tb−Tm)/(Ts−Tm)·100, StallPointOs=StallPoint·(1+Запас/100).
- **11130 Размер трубы по скорости, упрощ. (1)** — ✅ формула в движке (дублирует calc-2, отд. страница не нужна).

**ФАЗА 1 ЗАВЕРШЕНА.** Доступно калькуляторов: 17 базовых + 13 конверторов + stall = ~31 страница.

## Фаза 2 — Вода и Воздух/Газ: гидравлика (18 шт.)
Переиспользуют движок труб и модель клапанов. Нужны свойства воды и воздуха (блоки выше).
- **Вода 13110–13150 (5)** — ✅ ГОТОВО. Страницы calc-19…23. Свойства воды: ρ из IF97 (область 1),
  μ=2.414e-5·10^(247.8/(T−140)), T_воды=20°C по умолч. Поправка трения 1.006 (как у пара).
  Сверено с TLV: DN50/1.19073/0.300 бар (−0.24%). Helper `hydraulics(v,...)` в движке.
- **Вода 13200, 13300, 13400 (3)** — ✅ ГОТОВО (calc-24..26). `Qw=0.0865·Cv·√(Δp/SG)`+кавитация (FL=0.9).
  Сверено с TLV: Qw=12.24 (+0.08%).
- **Воздух/газ 14110–14160, 14200, 14300, 14400 (9)** — ✅ ГОТОВО (calc-27..35). ρ воздуха=P/(287.05·T),
  μ Сазерленд, ρ газа=P·M/(RT); клапан 4.17·Cv·p1·{...}, AIR_FXT=0.72. Сверено с TLV: 14110 Δp −0.04%,
  14200 клапан 11.9094 (точно). 14140 — газ с вводом мол. массы и вязкости.

**ФАЗА 2 ЗАВЕРШЕНА** (кроме 13500 антиконденсат → Фаза 4). Всего ~48 страниц. Меню calc-main и index обновлены.
- **ГЕНЕРАТОР СТРАНИЦ построен** (%TEMP%/gen-calc-page.cjs): спец {file,code,title,fields[],advanced[],outputs[]};
  виды полей grade/size/select/measure/number, выходы string/measure. Грид-вложенность правильная.

## Фаза 3 — Котёл, стоимость, рекуперация (15 шт.)
- **11410/11420/11430 + 11440/11450 + 11460/11461 (7)** — ✅ ГОТОВО (calc-36..42), сверено с TLV
  (КПД 75.179, Ce 0.99757, Cs 2415, сухость ΔT 6.908, смесь точно). hfw=region1(0.101325,Tfw).
  Сухость: ΔT=(h1−hg2)/cp(Ts2); смесь: ps=p·(1−a/100), выходы tempDrop в tempDiff.
- **12210–12230 Конденсатопроводы (4)** — гидравлика конденсата с учётом вскипания (Vtemp). TODO.
- **12110–12140 Рекуперация тепла конденсата (4)** — экономика теплоутилизации (много входов). TODO.

## Фаза 4 — Сложная физика / калибровка (4 шт.)
- **11470 Эконом. толщина изоляции** — оптимизация (стоимость изоляции vs стоимость теплопотерь).
- **13500 Антиконденсатная изоляция** — расчёт толщины по точке росы.
- **14500 Осушение сжатого воздуха** — психрометрия (конденсат из сжатого воздуха).
- **14600 Свойства влажного воздуха** — психрометрическая таблица.

---

## Рекомендация по порядку
1. Блоки: генератор страниц + свойства воды/воздуха.
2. Фаза 1 (быстрый видимый прогресс: +16).
3. Фаза 2 (вода+воздух: +18) — крупнейшая группа, максимум переиспользования.
4. Фаза 3 (котёл/рекуперация: +15).
5. Фаза 4 (4 сложных).

## Принятые решения
- **Вёрстка: автогенерация** — `calc-N.pug` генерируются из `calculators.json` по единому шаблону
  (дизайн как у текущих 17), верстальщик не нужен.
- **Старт: Фаза 1** (конверторы единиц + stall point + 11130).

## Очередь работ (по принятым решениям)
1. Генератор pug-страниц по шаблону + новые категории единиц.
2. Фаза 1 → Фаза 2 → Фаза 3 → Фаза 4.
